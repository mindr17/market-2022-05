{
  "name": "name",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start": "npx ts-node-dev --respawn --transpile-only ./src/index.ts",
    "build": "rm -rf ./dist/ && npx tsc --rootDir src --outDir dist",
    "build-not-implemented": "rm -rf ./dist/ && tsc",
    "lint": "npx eslint src/ --ext ts"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "mongodb": "^4.6.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.2",
    "websocket": "^1.0.34"
  },
  "devDependencies": {
    "@types/node": "^17.0.36",
    "@types/websocket": "^1.0.5"
  }
}
